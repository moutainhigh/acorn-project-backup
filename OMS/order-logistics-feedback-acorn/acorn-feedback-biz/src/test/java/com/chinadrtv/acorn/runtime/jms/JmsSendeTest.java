package com.chinadrtv.acorn.runtime.jms;


//@RunWith(SpringJUnit4ClassRunner.class)
//@ContextConfiguration(locations = { "classpath*:/spring/applicationContext-*-send.xml" })
public class JmsSendeTest {
//    private Logger      loggger = LoggerFactory.getLogger(Const.LOG_TYPE.PAFF_COMMON.val);

 /*   @Autowired
    private SendMessage sampleSend;

    @Test
    public void testSendTopicMessage() {
        for (int i = 1; i <= 4; i++) {
            String topicMsg = "发送主题 消息 " + i + " topic!!!";
            sampleSend.sendTopic(topicMsg);
            loggger.info(topicMsg);
        }
    }

    @Test
    public void testSendQueueMessage() {
        for (int i = 0; i < 3; i++) {
            String queueMsg = "发送队列 消息 " + i + " topic!!!";
            sampleSend.sendQueue(queueMsg);
            loggger.info(queueMsg);
        }
    }*/

}
