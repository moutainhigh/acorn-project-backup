<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chinadrtv.dal.iagent.dao.CityAllDao">

	<select id="queryById" parameterType="java.lang.Integer" resultType="CityAll">
		select * from iagent.city_all c where c.cityid = #{cityId}
	</select>
</mapper>